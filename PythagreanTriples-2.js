let side1  =  0;
let side2  =  0;
let side1squared = side1 * side1;
let side2squared = side2 *  side2;
let hypotenuse  =  Math.sqrt(side1squared+side2squared);

function pythagoreanTriples(side1, side2, hypotenuse){
for(side1  =  1, side1  <=  500,  side1++){
side1 *= side1; // Squaring Side 1
hypotenuse = side1 * side2
  for (side2  =  2, side2<=500,  side2++){
side2 *= side2; // Squaring Side 1
  for (hypotenuse <= 500){
    if  ()
    switch(hypotenuse){
      case hypotenuse.isInteger():
      console.log(side1, side2, hypotenuse);
      break;

      default:
      break;
  

}
}

}


}

}
}